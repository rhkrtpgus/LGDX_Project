# TEST FLOW

기준일: 2026-03-16

## 1. 기본 상태 확인

아래 주소가 모두 열리는지 먼저 확인한다.

- `http://localhost/`
- `http://localhost/api/settings/runtime`
- `http://localhost/fastapi/system/health`
- `http://localhost:4173/`
- `http://localhost:4174/`

기대 결과:

- webOS Front가 열린다.
- Spring API가 `200` 응답을 준다.
- FastAPI health가 `backend: UP`, `database: UP`으로 나온다.
- webOS UI와 모바일 UI가 각각 정상 표시된다.

## 2. webOS 메인 흐름

1. `http://localhost:4173/` 접속
2. 상단 로고가 모든 화면에서 보이는지 확인
3. 로고 클릭 시 메인으로 돌아오는지 확인
4. 메인 상단 배너 클릭 반응 확인
5. 앱 아이콘 6개 링크 확인

기대 결과:

- 상단 로고는 헤더 안에 잘리지 않고 표시된다.
- 로고 클릭 시 항상 홈으로 이동한다.
- 상단 배너는 각각 `시청 기록`, `ThinQ 보기`로 이동한다.
- 넷플릭스, 왓챠, 웨이브, 티빙, 유튜브, 쿠팡플레이 아이콘이 정상 표시된다.

## 3. 아이들TV 진입 흐름

1. 메인에서 `아이들TV` 또는 키즈 진입 버튼 클릭
2. 자녀 선택 화면 확인
3. 자녀 선택 후 맞춤형 키즈 화면 진입
4. 프로필 변경 버튼으로 다시 선택 화면 이동

기대 결과:

- 자녀 선택 화면이 먼저 뜬다.
- 선택한 자녀 기준 화면이 열린다.
- 다시 들어가면 마지막 선택 자녀 기준으로 이어진다.

## 4. 가족/자녀 전역 동기화

1. `설정`에서 가족과 자녀를 바꾼다.
2. `아이들TV`로 이동한다.
3. `유튜브 확인`으로 이동한다.
4. 새로고침한다.

기대 결과:

- 설정에서 고른 가족/자녀가 전체 화면에 반영된다.
- 아이들TV, 유튜브 확인, 가족 보호 모두 같은 자녀 기준으로 열린다.
- 새로고침 후에도 선택이 유지된다.

## 5. 유튜브 보호 흐름

1. 메인 또는 아이들TV에서 `유튜브` 진입
2. 허용 카테고리만 먼저 보이는지 확인
3. 추천 카드가 자동 검증되는지 확인
4. 통과한 카드만 남는지 확인
5. 차단된 카드가 숨김/사유 표시되는지 확인

기대 결과:

- 유튜브 화면 진입 시 카테고리 필터가 먼저 적용된다.
- 추천 영상은 분석 API로 먼저 검증된다.
- `NudeNet`, `Violent`, 카테고리 필터를 통과한 카드만 보여야 한다.
- 차단된 카드가 있다면 숨김 수와 사유가 화면에 표시된다.

## 6. addiction.py 실시간 모니터 흐름

사전 조건:

- `설정`에서 개인정보 동의 `ON`
- 중독 모니터 `ON`
- 자녀 보호 `ON`
- 로컬 FastAPI 실행

실행 순서:

1. `C:\Users\4121\Desktop\DX\LGDX_Project\FastAPI\scripts\start-fastapi-camera.ps1` 실행
2. `http://127.0.0.1:8000/fastapi/system/health` 확인
3. webOS에서 유튜브 진입
4. 허용 영상 선택
5. 실시간 시청 케어 카드 확인
6. `카메라 종료` 버튼 동작 확인

기대 결과:

- `monitor/start -> active -> live -> stop` 흐름이 동작한다.
- `blinkBpm`, `focusScore`, `riskScore`, `childMessageCard`가 `live` 응답에 보인다.
- 아이들 모드에서는 곰돌이 팝업이 뜬다.
- 눈 깜박임 부족 시 전체 화면 비눗방울 오버레이가 뜬다.
- 깜박임이 회복되면 비눗방울이 터지듯 사라진다.

## 7. TV 시청 리포트 화면

1. 메인에서 `시청 기록` 또는 상단 이어보기 배너 클릭
2. `TV 시청 리포트` 화면 확인
3. 이어보기 버튼이 있는 항목 클릭

기대 결과:

- 노란 배경 리포트 화면이 정상 표시된다.
- 최근 시청, 최근 알림, 누적 시간, 사전 확인 카드가 보인다.
- 유튜브 분석 이력과 연결된 항목은 `이어서 보기` 버튼으로 실제 URL 이동이 가능하다.

## 8. ThinQ 대시보드

1. 메인에서 `ThinQ 보기` 클릭
2. 내부 대시보드 화면 확인
3. 가족 보호와 유튜브 필터 섹션 확인

기대 결과:

- 외부 페이지가 아니라 내부 대시보드가 열린다.
- 최근 알림, 리포트 요약, 자녀별 보호 상태가 보인다.
- ThinQ 화면에서도 가족 보호와 유튜브 필터를 바로 조절할 수 있다.

## 9. 모바일 UI 홈

1. `http://localhost:4174/` 접속
2. 홈 상단 자녀 카드 확인
3. 홈의 `가족 보호` 섹션 확인
4. 설정 상세로 이동

기대 결과:

- 자녀 카드에서 오늘 시청, 남은 시간, 자동 차단 상태가 보인다.
- 홈에 `가족 보호` 섹션이 보인다.
- 일일 제한, 취침 잠금, 자동 차단, 보호자 알림, 콘텐츠 등급, 스마트캠 상태, 요일별 시청 제한 미리보기가 나온다.
- `가족 보호 설정 자세히 보기` 버튼으로 설정 화면으로 이동한다.

## 10. 배포 전 체크

정리 기준:

- 로그 파일은 커밋하지 않는다.
- `__pycache__`, `*.pyc`, 임시 가상환경은 커밋하지 않는다.
- FastAPI 카메라 환경은 `requirements-monitor.txt` 기준으로 재생성한다.

운영 체크:

- 첫 초기화 후 `SPRING_SQL_INIT_MODE=never`
- `POSTGRES_PASSWORD` 운영값 반영
- 서버에서 `start-fastapi-camera.ps1` 대신 운영 방식에 맞는 Python 실행 경로 지정
